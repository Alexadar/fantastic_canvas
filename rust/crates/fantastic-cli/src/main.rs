//! `fantastic` CLI — composes the kernel + default bundle set.
//!
//! Phase 1 stub. Real composition (mode dispatch, REPL, daemon,
//! one-shot RPC, install-bundle) lands with task #229. Until then this
//! binary just prints a placeholder so `cargo build --bin fantastic`
//! produces something runnable for the workspace integration tests.

fn main() {
    eprintln!(
        "fantastic (Phase 1 scaffold): kernel + bundle impl pending. \
         See ../README.md for the build instructions and roadmap."
    );
}
