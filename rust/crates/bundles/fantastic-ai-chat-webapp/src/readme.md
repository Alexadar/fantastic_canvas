# ai_chat_webapp — provider-agnostic chat UI
Fronts any LLM backend that answers send / history / interrupt. `provider` field picks the backend bundle. Rust port phase 1: provider auto-spawn deferred — set `upstream_id` manually via `update_agent`.
