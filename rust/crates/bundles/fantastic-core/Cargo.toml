[package]
publish = false
name = "fantastic-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Root orchestrator bundle — the userland id=\"core\" agent at the top of the tree."

[dependencies]
[dev-dependencies]
tempfile = "3"
