# telemetry_pane — live agent-vis GL view
Renders agents as water-floating sprites + sender→receiver neon wires + traffic pulses + a last-10 message log. Add to any canvas via add_agent; consumes the kernel state stream (pure consumer — never calls back).
