//! UniFFI-bound Swift surface for embedding the Rust kernel.
//!
//! Phase 3 deliverable. The UDL + bindings generation land alongside
//! `scripts/build-xcframework.sh` and the Swift package wrapper in
//! `rust/packaging/FantasticKernel/`. Keeping this crate as an empty
//! shell so the workspace compiles cleanly today.

#![deny(missing_docs)]
