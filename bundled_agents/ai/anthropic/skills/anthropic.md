# Anthropic Agent

Connects to Claude API via the `anthropic` SDK.

## Config

- `model`: e.g. `claude-sonnet-4-20250514`
- `api_key`: From `ANTHROPIC_API_KEY` env var

200k context window for all Claude models.
