# ssh_runner — remote fantastic lifecycle over SSH
Verbs: start, stop, restart, status. Drives a remote `fantastic` + opens a local SSH tunnel so a remote canvas can be iframed locally. Composes with kernel_bridge for messaging.
