# Voice Assistant agent — no server-side code needed.
# All voice logic runs in the browser (STT/TTS).
# Backend handles AI connector dispatch via WS.
print("Voice assistant active. Voice I/O runs in browser.")
