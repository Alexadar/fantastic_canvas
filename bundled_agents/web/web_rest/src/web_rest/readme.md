# web_rest — HTTP verb channel (diagnostic)
Child of a web agent. `POST /<self_id>/<target_id>` body=payload → kernel.send → JSON. Browser-pastable shortcuts: `GET /<self_id>/_reflect[/<target>][?readme=1]`. Multiple instances coexist.
