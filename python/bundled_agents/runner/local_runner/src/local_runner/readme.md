# local_runner — local sub-fantastic lifecycle
Verbs: start, stop, restart, status, get_webapp. Manages a `fantastic` daemon for one project dir. Truth read from the project's `.fantastic/lock.json` (PID) + its web agent record (port).
