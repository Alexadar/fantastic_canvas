# ssh_runner — remote fantastic lifecycle over SSH
Verbs: start, stop, restart, status. Drives a remote `fantastic` + opens a local SSH tunnel that exposes the remote daemon's HTTP port on a local address. Composes with kernel_bridge for messaging.
